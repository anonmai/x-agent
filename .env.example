# 网络代理（如需代理访问 X API）
HTTPS_PROXY=http://127.0.0.1:
HTTP_PROXY=http://127.0.0.1:

# OAuth 2.0 Application-Only（搜索接口：recent/all）
# 在开发者门户生成 App Bearer Token。
X_APP_BEARER_TOKEN=

# OAuth 2.0 User Context（发帖/用户态接口）
# 在开发者门户创建 OAuth 2.0 应用，完成用户授权流程后填入；与 App-only Bearer 不同。
X_OAUTH2_ACCESS_TOKEN=

# 可选：仅用 Refresh Token 换 Access Token（需授权时勾选 offline.access；保密应用需 Client Secret）
X_CLIENT_ID=
X_CLIENT_SECRET=
X_OAUTH2_REFRESH_TOKEN=
